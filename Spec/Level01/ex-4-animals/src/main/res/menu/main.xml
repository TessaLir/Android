<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <item
        android:id="@+id/main.search"
        android:title="@string/menu_main_search"
        android:icon="@drawable/ic_search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <item
        android:id="@+id/main.up"
        android:title="@string/menu_main_up"
        android:icon="@drawable/ic_arrow_upward"
        app:showAsAction="always"/>

    <item
        android:id="@+id/main.down"
        android:title="@string/memu_main_down"
        android:icon="@drawable/ic_arrow_downward"
        app:showAsAction="always"/>

    <item
        android:id="@+id/main.add"
        android:title="@string/menu_main_add"
        android:icon="@drawable/ic_add"
        app:showAsAction="ifRoom"/>

</menu>